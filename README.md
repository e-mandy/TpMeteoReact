# TP DASHBOARD METEO

This project is running a dashboard around the weather information for severals countries.

## RUN PROCESS

- Clone the project

  ```
  git clone git@github.com:e-mandy/TpMeteoReact.git
  ```

- Install dependencies

  ```
  npm install
  ```

- Launch the server

  ```
  npm run dev
  ```
